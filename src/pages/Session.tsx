import React from 'react'
import { Container, Typography, Box } from '@mui/material'
import RaiseHandTask from '../components/RaiseHandTask'

export default function Session() {
  return (
    <Container maxWidth="lg" sx={{ mt: 4, mb: 4 }}>
      <RaiseHandTask />
    </Container>
  )
}
